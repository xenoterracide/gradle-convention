# SPDX-FileCopyrightText: Copyright © 2024 - 2026 Caleb Cushing
#
# SPDX-License-Identifier: CC0-1.0

name: legacy-publish-manual
on:
  # This workflow has been superseded by the consolidated jobs in full.yml.
  # Keep it for manual runs only, to aid troubleshooting if ever needed.
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Publishing is now handled by the 'publish' job inside the 'full' workflow."
